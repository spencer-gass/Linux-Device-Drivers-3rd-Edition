# Example Module with procfs Entry

- Example module that creates a basic procfs file entry that prints hello-world

### Usage

```
insmod main.ko
cat /proc/proc_demo/proc_fs
cat /proc/proc_demo/proc_fs_mul
rmmod mail.ko
```